{
  "name": "portfolio-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "start": "node build.js && npx serve dist"
  },
  "engines": {
    "node": ">=14"
  }
}
